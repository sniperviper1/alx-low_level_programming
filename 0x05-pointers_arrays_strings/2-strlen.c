#include "main.h"
#include <string.h>

/**
* _strlen - to return the length of a string of a given function
* @s: parameter of a function
* Return: return the lenght if a string
*/
int _strlen(char *s)
{
return (strlen(s));
}
